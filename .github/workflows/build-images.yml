name: Build & Push All Images

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  DOCKERHUB_USER: yaswanth111

jobs:
  build-api-gateway:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: deployment/docker/api-gateway.Dockerfile
          push: true
          tags: ${{ env.DOCKERHUB_USER }}/tagent-api-gateway:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-discovery:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: deployment/docker/discovery.Dockerfile
          push: true
          tags: ${{ env.DOCKERHUB_USER }}/tagent-discovery:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-monitoring:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: deployment/docker/monitoring.Dockerfile
          push: true
          tags: ${{ env.DOCKERHUB_USER }}/tagent-monitoring:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-ai-engine:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: deployment/docker/ai-engine.Dockerfile
          push: true
          tags: ${{ env.DOCKERHUB_USER }}/tagent-ai-engine:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-remediation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: deployment/docker/remediation.Dockerfile
          push: true
          tags: ${{ env.DOCKERHUB_USER }}/tagent-remediation:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: deployment/docker/web.Dockerfile
          push: true
          tags: ${{ env.DOCKERHUB_USER }}/tagent-web:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
