apiVersion: v1
kind: ConfigMap
metadata:
  name: tagent-config
  namespace: tagent
data:
  REMEDIATION_MODE: "read-only"
  OLLAMA_MODEL: "llama3.1:8b"
  OLLAMA_EMBEDDING_MODEL: "nomic-embed-text"
  PROMETHEUS_URL: "http://prometheus-server.monitoring:9090"
