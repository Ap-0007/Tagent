# Tagent Helm Chart — Default Values
# One command: helm install tagent tagent/tagent --namespace tagent --create-namespace

createNamespace: false

# ===== SERVICES =====

apiGateway:
  replicaCount: 1
  image:
    repository: ghcr.io/tagent-dev/api-gateway
    tag: "latest"
    pullPolicy: Always
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits: { cpu: 500m, memory: 256Mi }

discovery:
  image:
    repository: ghcr.io/tagent-dev/discovery
    tag: "latest"
    pullPolicy: Always
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits: { cpu: 500m, memory: 256Mi }

monitoring:
  image:
    repository: ghcr.io/tagent-dev/monitoring
    tag: "latest"
    pullPolicy: Always
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits: { cpu: 500m, memory: 256Mi }

aiEngine:
  image:
    repository: ghcr.io/tagent-dev/ai-engine
    tag: "latest"
    pullPolicy: Always
  resources:
    requests: { cpu: 200m, memory: 256Mi }
    limits: { cpu: 1000m, memory: 512Mi }

remediation:
  image:
    repository: ghcr.io/tagent-dev/remediation
    tag: "latest"
    pullPolicy: Always
  mode: "read-only"
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits: { cpu: 500m, memory: 256Mi }

notification:
  image:
    repository: ghcr.io/tagent-dev/notification
    tag: "latest"
    pullPolicy: Always
  resources:
    requests: { cpu: 50m, memory: 64Mi }
    limits: { cpu: 200m, memory: 128Mi }

web:
  replicaCount: 1
  image:
    repository: ghcr.io/tagent-dev/web
    tag: "latest"
    pullPolicy: Always
  apiUrl: "http://tagent-api-gateway:8080"
  service:
    type: ClusterIP
  resources:
    requests: { cpu: 50m, memory: 128Mi }
    limits: { cpu: 500m, memory: 512Mi }

# ===== LOCAL LLM (no cloud APIs) =====

ollama:
  enabled: true
  image:
    repository: ollama/ollama
    tag: latest
    pullPolicy: IfNotPresent
  model: "llama3.1:8b"
  embeddingModel: "nomic-embed-text"
  persistence:
    enabled: true
    size: 20Gi
    storageClass: ""
  resources:
    requests: { cpu: "1", memory: 6Gi }
    limits: { cpu: "4", memory: 12Gi }

# ===== NIGHT GUARDIAN =====

nightGuardian:
  enabled: false
  confidence: "85"

# ===== ESCALATION =====

escalation:
  phoneDelayMin: "3"
  autoFixDelayMin: "10"
  quietStart: "22:00"
  quietEnd: "06:00"

# ===== SECRETS =====

secrets:
  databaseUrl: ""
  redisUrl: ""
  slackWebhookUrl: ""
  smtpHost: ""
  smtpUser: ""
  smtpPassword: ""

# ===== INGRESS =====

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: tagent.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

# ===== RBAC =====

rbac:
  create: true
